defmodule SmeeView.AspectFilter do

  @moduledoc false


  alias SmeeView.Utils

#  def idp(enum, bool \\ true) do
#    enum
#    |> Stream.filter(fn e -> Entity.idp?(e) == bool end)
#  end
#
#  def localize(aspects, lang \\ Utils.default_lang()) do ## <- This needs aspect protocol in order to work with many modules
#    exact = Enum.filter(aspect, fn a -> Aspect.lang?(a, lang) end)
#
#
#  end

end
